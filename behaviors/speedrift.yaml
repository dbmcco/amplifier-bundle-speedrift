behavior:
  name: speedrift
  version: 0.3.0
  description: Workgraph-first Speedrift orchestration behavior with enforced pre/post drift checks.

agents:
  speedrift-coordinator:
    bundle: speedrift:agents/speedrift-coordinator.md

context:
  include:
    - speedrift:context/speedrift-protocol.md
    - speedrift:context/model-routing.md
